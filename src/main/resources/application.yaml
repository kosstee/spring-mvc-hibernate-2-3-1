spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kata
    username: root
    password: password
  jpa:
    show-sql: on
    hibernate:
      ddl-auto: update
    open-in-view: off